process_names:
  - name: "cookieserver"
    cmdline:
      - "cookieserver"
      - "./bin/cookieserver"

  - name: "cookieclient"
    cmdline:
      - "cookieclient"
      - "-e ./exploits/"

  - name: "df_server"
    cmdline:
      - "python3 -m flask run --host 0.0.0.0"

  - name: "df_client"
    cmdline:
      - "python3 ./start_sploit.py"
